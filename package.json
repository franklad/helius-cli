{
  "name": "helius-cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Solana CLI for Helius APIs — built with incur",
  "bin": {
    "helius": "./bin/helius.js"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "bun dist/cli.js"
  },
  "dependencies": {
    "incur": "^0.1.17"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.7.0"
  }
}
